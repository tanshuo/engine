define start;
    declare function fun1 as 1;
    declare function fun2 as 1;
    declare variable A as vector3;
    declare variable B as vector3;
enddef;

define update;
    A set by  <1,2,3>;
    if A is <1,2,3>,
        A move to <6,7,8>,
        if A greater than <5,6,7>,
            kill B,
        end,
    end;
enddef;
